let val;
val = Math.PI;
val = Math.E;
val = Math.round(2.4); // If the value is 2.4 then it will return 2 if the value is 2.8 then it will return 3
val = Math.ceil(2.4); // It will round up and return 3
val = Math.floor(2.8); // It will round down and return 2
val = Math.sqrt(64);
val = Math.abs(-1);
val = Math.pow(8, 2);
val = Math.max(1, 2, 4, 56, 4, 5);
val = Math.min(1, 2, 4, 56, 4, 5);
val = Math.random();
val = Math.random() * 20 + 1;
console.log(val);
